import { colors } from './_colors.styles';
import { fonts } from './_fonts.styles';
import { media } from './_media.styles';

export const theme = {
    colors,
    media,
    fonts,
};
export type ITheme = typeof theme;
